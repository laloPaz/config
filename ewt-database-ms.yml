variables:
  eureka:
    #host: localhost
    host: service-registry-host
    port: 8761
info:
  component: Microservicio de Base de Datos
#server:
#  port: 7706
spring:
  config:
    name: ewt-database-ms
  application:
    name: ewt-database-ms
  cloud:
    services:
      registrationMethod: direct

security:
  basic:
    enabled: false
    
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://${variables.eureka.host}:${variables.eureka.port}/eureka/
#    registerWithEureka: true
#  instance:
#    preferIpAddress: true

logging:
  level:
    org.springframework.web: INFO
    com.banamex.portal.microservice.ewtdatabasems: DEBUG

#db.driverClass: oracle.jdbc.OracleDriver
#db.jdbcUrl: jdbc:oracle:thin:@34.230.196.141:1521:xe
#db.jdbcUrl: jdbc:oracle:thin:@172.17.0.1:1521:xe
#db.user: EWT
#db.password: password

#c3p0.minPoolSize: 4
#c3p0.maxPoolSize: 20
#c3p0.acquireIncrement: 2
#c3p0.breakAfterAcquireFailure: false
#c3p0.maxIdleTime: 10



db.driverClass=${vcap.services.user-provided.credentials.driver-class}
db.jdbcUrl=${vcap.services.user-provided.credentials.jdbc-url}
db.user=${vcap.services.user-provided.credentials.user}
db.password=${vcap.services.user-provided.credentials.password}

c3p0.minPoolSize=${vcap.services.user-provided.credentials.min-pool-size}
c3p0.maxPoolSize= ${vcap.services.user-provided.credentials.max-pool-size}
c3p0.acquireIncrement= ${vcap.services.user-provided.credentials.acquire-increment}
c3p0.breakAfterAcquireFailure=${vcap.services.user-provided.credentials.break-after-acquire-failure}
c3p0.maxIdleTime=${vcap.services.user-provided.credentials.max-idle-time}
